databases:
  - name: doughfin_be
    databaseName: doughfin_be_db
    user: admin
    plan: free

services:
  - type: web
    name: doughfin_be
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    # preDeployCommand: "./bin/rails db:migrate" # preDeployCommand only available on paid instance types
    startCommand: "./bin/rails server"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: dough_fin_be_production
          property: postgres://dough_fin_be:p2tg0esPtBNv8Hp1xtPdtssFLGL8WMnr@dpg-cn0kgbi1hbls739g90v0-a/dough_fin_be_production
      - key: RAILS_MASTER_KEY
        sync: false
      - key: WEB_CONCURRENCY
        value: 2 # sensible default